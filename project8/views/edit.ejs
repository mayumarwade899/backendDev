<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="text-zinc-500" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your mind?" name="content" class="resize-none bg-zinc-800 block rounded-md border-none outline-none p-3 w-1/3"><%= post.content%></textarea>
            <input class="px-3 py-2 w-30 bg-yellow-500 text-black block rounded-md mt-2 cursor-pointer" type="submit" value="Update post">
        </form>
    </div>
</body>
</html>